import { NodePortDirective } from './node-port.directive';

describe('NodePortDirective', () => {
  /*it('should create an instance', () => {
    const directive = new NodePortDirective();
    expect(directive).toBeTruthy();
  });*/
});
