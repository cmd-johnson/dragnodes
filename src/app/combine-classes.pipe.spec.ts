import { CombineClassesPipe } from './combine-classes.pipe';

describe('CombineClassesPipe', () => {
  it('create an instance', () => {
    const pipe = new CombineClassesPipe();
    expect(pipe).toBeTruthy();
  });
});
